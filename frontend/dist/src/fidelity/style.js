// STUB — filled in by its own task. A SerializerPolicy, not a Preservation:
// it configures the serializer rather than transforming its output.
export const markdownStyle = {
  name: 'markdownStyle',
  detect: () => ({}),
}
