// STUB — filled in by its own task. Registered now so the registry's imports
// resolve and the port contract can be asserted before behaviour moves.
export const breaks = {
  name: 'breaks',
  capture: (markdown) => ({ state: null, markdown }),
  restore: (text) => text,
}
